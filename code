#include <stdio.h>

int main(void) {
	int t,n,x,y,z,count,i,j,a,b,c;
	scanf("%d",&t);
	while(t--)
	{
	  scanf("%d %d",&x,&y);
	  if(x<y)
      printf("PROFIT\n");
    else if(x>y)
       printf("LOSS\n");
    else
       printf("NEUTRAL\n");
	}
	return 0;
}

